// Note: @web/config-loader has a Windows path resolution issue that prevents
// this config file from being auto-loaded. The globs and options are passed
// directly via CLI flags in the `build:cem` npm script instead.
//
// Reference (kept for documentation):
// export default {
//   globs: ['src/audio-analyzer.ts', 'src/audio-visualizer.ts', 'src/index.ts'],
//   litelement: true,
//   packagejson: true,
// };
